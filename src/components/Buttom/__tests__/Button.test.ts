import React from 'react';
import {render, fireEvent, screen} from '@testing-library/react-native';

import Button, {ButtonProps} from '../../Buttom';

describe('Button', () => {
  it('renders correctly', () => {});
});
